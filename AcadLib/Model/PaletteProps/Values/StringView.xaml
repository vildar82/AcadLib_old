<UserControl x:Class="AcadLib.PaletteProps.StringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AcadLib.PaletteProps"
             xmlns:wpf="clr-namespace:NetLib.WPF;assembly=NetLib"
             xmlns:paletteProps="clr-namespace:AcadLib.PaletteProps"
             xmlns:converters="clr-namespace:NetLib.WPF.Converters;assembly=NetLib"
             d:DataContext="{d:DesignInstance paletteProps:StringVM}"
             mc:Ignorable="d">
    <Grid>
        <TextBox Text="{Binding Value}" IsReadOnly="{Binding IsReadOnly}"
                 BorderThickness="0"
                 local:InputBindingsManager.UpdatePropertySourceWhenEnterPressed="TextBox.Text" />
        <TextBlock Text="{x:Static local:PalletePropsService.Various}" IsHitTestVisible="False"
                   Visibility="{Binding IsVarious, Converter={converters:BooleanToCollapseVisibilityConverter}}"
                   Foreground="{DynamicResource TextReadBrush}"/>
    </Grid>
</UserControl>