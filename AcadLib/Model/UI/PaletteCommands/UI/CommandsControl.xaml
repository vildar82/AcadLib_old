<UserControl
    x:Class="AcadLib.PaletteCommands.UI.CommandsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:AcadLib.PaletteCommands.UI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    d:DataContext="{d:DesignInstance local:DesignTimePalette,
                                     IsDesignTimeCreatable=True}"
    mc:Ignorable="d">
    <Grid Background="{Binding Background}">
        <ItemsControl
            Background="Transparent" BorderThickness="0"
            ItemsSource="{Binding PaletteCommands}"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Width="40" Height="40" Command="{Binding Command}" 
                            Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                            Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">
                        <Image Margin="2" Source="{Binding Image}" />
                        <ToolTipService.ToolTip>
                            <StackPanel>
                                <TextBlock Margin="1" Text="{Binding Name}" />
                                <TextBlock Margin="1" Text="{Binding Description}" />
                            </StackPanel>
                        </ToolTipService.ToolTip>
                        <Button.ContextMenu>
                            <ContextMenu ItemsSource="{Binding ContexMenuItems}">
                                <ContextMenu.ItemContainerStyle>
                                    <Style TargetType="{x:Type MenuItem}">
                                        <Setter Property="Icon" Value="{Binding Icon}"/>
                                        <Setter Property="Header" Value="{Binding Name}"/>
                                        <Setter Property="Command" Value="{Binding Command}"/>
                                    </Style>
                                </ContextMenu.ItemContainerStyle>
                            </ContextMenu>
                        </Button.ContextMenu>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
