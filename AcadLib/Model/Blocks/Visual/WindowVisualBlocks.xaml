<Window x:Class="AcadLib.Blocks.Visual.WindowVisualBlocks"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AcadLib.Blocks.Visual"
        mc:Ignorable="d"
        Title="Выбор блока">
    <Window.Resources>
        <DataTemplate x:Key="ImageCell">
            <StackPanel Orientation="Vertical">
                <Image Source="{Binding Image}" Width="190" Margin="2"/>
                <TextBlock Text="{Binding Name}" HorizontalAlignment="Center" />
            </StackPanel>
        </DataTemplate>                
    </Window.Resources>    
    <Grid>
        <ListBox Name="ListBoxVisuals" ItemsSource="{Binding Visuals}"
                 SelectionMode="Single" ItemTemplate="{StaticResource ImageCell}"
                 Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Grid.RowSpan="2"                 
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"  Style="{x:Null}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>        
    </Grid>
</Window>
